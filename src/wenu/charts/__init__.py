"""Chart specifications, styles, and rendering results.

Public chart classes remain exported from the top-level ``wenu`` package.
Keeping this initializer lightweight also prevents chart styles from being
imported while the sky package is still initializing.
"""
